import { ArrangementsPipe } from './arrangements.pipe';

describe('ArrangementsPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrangementsPipe();
    expect(pipe).toBeTruthy();
  });
});
